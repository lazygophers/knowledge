# Golang 语言基础

## 核心约定

- 优先使用 `context.Context` 贯穿请求生命周期。
- 错误返回统一使用 `error`，避免字符串约定。
- 接口定义靠近调用方，而不是实现方。

## 基础清单

1. 类型系统与零值语义
2. 函数、方法与接收者
3. 接口与依赖注入
4. 错误处理与包装

## 实践建议

- 通过 `golangci-lint` 做静态检查。
- 在 PR 模板中加入“边界情况”自检项。
