import { Badge } from "@rspress/core/theme";

# Panic vs Error

<Badge text="核心概念" type="danger" />

Go 提供了两种错误处理机制。

## Error

预期的失败，返回给调用者处理。

## Panic

意外的灾难，程序可能终止。

## 总结

默认使用 error，极少使用 panic。
